let Logger,Server;_46a‍.w("./classes/Logger",[["default",["Logger"],function(v){Logger=v}]]);_46a‍.w("./classes/Server",[["default",["Server"],function(v){Server=v}]]);


/**
 * Define Application start
 * @returns {Promise<void>}
 */
const start = async () => {
    try {
        // Initialize the server
        await Server.init();
    } catch (err) {
        Logger.error(`Error while starting the app: ${err.message}`);
        Logger.error(`Error: %O`, err);

        throw err;
        process.exit(1);
    }
};

// Start the server
start();
