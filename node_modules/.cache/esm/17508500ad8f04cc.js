let Buffer,promisify,is,isFormData;_ad8‍.x([["default",()=>getBodySize]]);_ad8‍.w("node:buffer",[["Buffer",["Buffer"],function(v){Buffer=v}]]);_ad8‍.w("node:util",[["promisify",["promisify"],function(v){promisify=v}]]);_ad8‍.w("@sindresorhus/is",[["default",["is"],function(v){is=v}]]);_ad8‍.w("./is-form-data.js",[["default",["isFormData"],function(v){isFormData=v}]]);



               async function getBodySize(body, headers) {
    if (headers && 'content-length' in headers) {
        return Number(headers['content-length']);
    }
    if (!body) {
        return 0;
    }
    if (is.string(body)) {
        return Buffer.byteLength(body);
    }
    if (is.buffer(body)) {
        return body.length;
    }
    if (isFormData(body)) {
        return promisify(body.getLength.bind(body))();
    }
    return undefined;
}
