let Database,mongoose,Schema,Logger;_f9e‍.x([["default",()=>_f9e‍.o]]);_f9e‍.w("../classes/database_veritas",[["default",["Database"],function(v){Database=v}]]);_f9e‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}],["Schema",["Schema"],function(v){Schema=v}]]);_f9e‍.w("../classes/Logger",[["default",["Logger"],function(v){Logger=v}]]);





const inviteModel = {
  /**
   * Init  schema
   */
  init() {
    const db = _f9e‍.a("Database",Database).getConnection();

    
    const inviteSchema = new (_f9e‍.a("mongoose",mongoose)).Schema({
        code: {
            type: "String"
        },
        email: {
            type: "String",
            default: null,
            required: true,
        },
        invitedBy: {
            type: _f9e‍.a("Schema",Schema).ObjectId,
            ref: "User"
        },
        type: {
            type: String,
            enum: ['user','organization'],
            default: 'user'
        },
        role: {
            type: String,
            enum: ['user', 'admin', 'superadmin'], // These are the roles available
            default: 'user'
        },
        organization: {
            type: _f9e‍.a("Schema",Schema).ObjectId,
            ref: "Organization"
        },
        acceptedAt: {
            type: "Date",
            default: null,
        },
        acceptedBy: {
            type: _f9e‍.a("Schema",Schema).ObjectId,
            ref: "User"
        },
        created: {
            type: "Date",
            default: null,
        },
        expires: {
            type: "Date",
            default: null,
        },

        updated: {
            type: "Date",
            default: null,
        },

    });

    inviteModel.setModel(_f9e‍.a("mongoose",mongoose).model("Invite", inviteSchema));
    return inviteSchema;

  },

  /**
   * Set Model
   */
  setModel: model => {
    inviteModel.model = model;
  },

  /**
   * Get model
   */
  getModel: () => {
    return inviteModel.model;
  },

  // Start queries


  // CRUD METHODS


  async create(item) {
    const obj = new inviteModel.model(item);
    return await obj.save();
  },

  async delete(id) {
    return await inviteModel.model.findByIdAndRemove(id);
  },

  
  async get(id) {
    return await inviteModel.model.findOne({ _id : id });
  },

  /**
  * OrganizationModel.list
  *   @description CRUD ACTION list
  *
  */
  async list() {
    return await inviteModel.model.find();
  },

  async getByOrg(orgId) {
    return await inviteModel.model.find({ organization: orgId });
  },

  async getByType(type) {
    return await inviteModel.model.find({ type: type });
  },

  /**
  * OrganizationModel.update
  *   @description CRUD ACTION update
  *   @param ObjectId id Id
  *
  */
  async update(item) {
    return await inviteModel.model.findOneAndUpdate({ _id: item._id }, item);
  },

  async findByCode(c) {
        return await inviteModel.model.findOne({
            code: c
        }).populate('organization');
  },

 



};

_f9e‍.d(inviteModel);_f9e‍.j(["default"]);
